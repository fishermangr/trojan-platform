# AES-128 Side-Channel Analysis Platform

A complete platform for AES-128 ECB encryption on FPGA with power trace capture, built using:

- **Google XLS** — High-Level Synthesis (C++ → Verilog RTL)
- **Xilinx Vivado** — FPGA synthesis, implementation & bitstream generation
- **ChipWhisperer Husky** — Power trace capture & target communication
- **CW312-A35** (Artix-7 XC7A35T) on **CW313** carrier board — FPGA target

---

## Directory Structure

```
AES-trojan-v2/
├── hls/                            # HLS: C++ → Verilog via Google XLS
│   ├── src/                        #   C++ source files
│   │   ├── aes_xls.h              #     Header: types, S-box, helpers
│   │   └── aes_xls.cc             #     AES-128 ECB (xlscc-compatible)
│   ├── run_hls.py                  #   HLS pipeline script
│   ├── aes_hls.bzl                 #   Bazel build rules (optional)
│   └── output/                     #   (Generated) IR artifacts
├── fpga/                           # FPGA: Vivado synthesis & implementation
│   ├── rtl/                        #   All RTL sources
│   │   ├── cw/                     #     ChipWhisperer reference HDL
│   │   │   ├── ss2_aes_wrapper.v   #       Top-level: SS2 + CW305 registers
│   │   │   ├── ss2.v              #       SimpleSerial2 protocol handler
│   │   │   ├── cw305_top.v        #       Register front-end + clocks
│   │   │   ├── cw305_reg_aes.v    #       AES register file
│   │   │   ├── cw305_usb_reg_fe.v #       Address decoder
│   │   │   ├── cw305_aes_defines.v#       Register address definitions
│   │   │   ├── clocks.v           #       Clock mux (IBUFG/BUFG)
│   │   │   ├── uart_core.v        #       UART TX/RX for SS2
│   │   │   ├── crc_ss2.v          #       CRC for SS2 framing
│   │   │   ├── fifo_sync.v        #       Synchronous FIFO
│   │   │   └── cdc_pulse.v        #       Clock domain crossing
│   │   ├── xls_aes_adapter.v      #     CW305 start/done ↔ XLS ready/valid
│   │   └── aes_128.sv             #     (Generated by HLS) XLS AES-128 core
│   ├── constraints/                #   Pin & timing constraints
│   │   └── cw312_a35.xdc          #     XDC for SS2 wrapper on CW312-A35
│   ├── scripts/                    #   Build scripts
│   │   └── synthesize.tcl          #     Full synth + impl + bitstream
│   └── build/                      #   (Generated) Vivado output
│       └── ss2_aes_wrapper.bit     #     Bitstream
├── capture/                        # Capture: communicate with FPGA + collect traces
│   ├── cw_aes.py                   #   Main CLI: encrypt/decrypt + trace capture
│   ├── upload_bitstream.py         #   Upload bitstream + verify via SS2
│   └── test_ss2.py                 #   Hardware communication debug tool
├── results/                        # Results: output data (.mat, .json)
│   └── <experiment_name>/          #   Subdirectories per experiment (via --results-dir)
├── docs/                           # Documentation
│   ├── README.md                   #   This file
│   └── commands.md                 #   Command reference
└── requirements.txt                # Python dependencies
```

---

## Prerequisites

| Tool | Version | Purpose |
|------|---------|---------|
| Google XLS | Latest (compiled from source) | HLS: C++ → Verilog |
| Vivado | 2024.x / 2025.x | FPGA synthesis & bitstream |
| Python | 3.8+ | Scripts & communication |
| ChipWhisperer Husky | Connected via USB | Power trace capture |
| CW312-A35 + CW313 | Plugged into Husky | FPGA target board |

### Python Dependencies

```bash
pip install -r requirements.txt
```

Required packages: `chipwhisperer`, `pycryptodome`, `scipy`, `numpy`

---

## Quick Start

### 1. Run HLS (C++ → Verilog)

```bash
cd AES-trojan-v2/hls
python3 run_hls.py
```

This compiles `src/aes_xls.cc` through the XLS toolchain:
1. **xlscc**: C++ → XLS IR (output: `output/aes_128.ir`)
2. **opt_main**: Optimize XLS IR (output: `output/aes_128.opt.ir`)
3. **codegen_main**: XLS IR → SystemVerilog (output: `../fpga/rtl/aes_128.sv`)

### 2. Synthesize & Generate Bitstream (Vivado)

```bash
cd AES-trojan-v2/fpga
vivado -mode batch -source scripts/synthesize.tcl
```

This runs the full Vivado flow:
1. Reads CW reference HDL from `rtl/cw/` (with `SS2_WRAPPER` and `XLS_AES` defines)
2. Reads adapter + XLS core from `rtl/` (`xls_aes_adapter.v` + `aes_128.sv`)
3. Applies `constraints/cw312_a35.xdc` for `ss2_aes_wrapper` top module
4. Synthesizes, places, routes
5. Generates bitstream → `build/ss2_aes_wrapper.bit`

### 3. Upload Bitstream to FPGA

```bash
cd AES-trojan-v2/capture
python3 upload_bitstream.py --bitstream ../fpga/build/ss2_aes_wrapper.bit
```

### 4. Capture Power Traces

```bash
cd AES-trojan-v2/capture

# Single encryption
python3 cw_aes.py encrypt

# 1000 random traces saved to results/exp1/
python3 cw_aes.py encrypt --random --num-traces 1000 \
    --bitstream ../fpga/build/ss2_aes_wrapper.bit \
    --results-dir ../results/exp1
```

---

## RTL Architecture

```
                    CW312-A35 FPGA (XC7A35T)
  ┌─────────────────────────────────────────────────────┐
  │  ss2_aes_wrapper (top)                              │
  │  ┌───────┐   ┌──────────────────────────────────┐   │
  │  │ SS2   │   │  cw305_top                        │   │
  │  │ UART  │──→│  ┌────────────┐  ┌─────────────┐ │   │
  │  │ COBS  │   │  │ usb_reg_fe │  │ reg_aes     │ │   │
  │  │ CRC   │←──│  │ (addr dec) │──│ (key/pt/ct) │ │   │
  │  └───────┘   │  └────────────┘  └──────┬──────┘ │   │
  │  rxd (IO2)   │                         │        │   │
  │  txd (IO1)   │  ┌──────────────────────┴──────┐ │   │
  │              │  │ xls_aes_adapter             │ │   │
  │              │  │ (start/done ↔ rdy/vld)      │ │   │
  │              │  │  ┌────────────────────────┐  │ │   │
  │              │  │  │ aes_128 (XLS-generated)│  │ │   │
  │              │  │  └────────────────────────┘  │ │   │
  │              │  └──────────────────────────────┘ │   │
  │              └──────────────────────────┬────────┘   │
  │                                        │ TRIGGER    │
  │  CLK ← HS2 (7.37 MHz from Husky)      └──→ IO4     │
  └─────────────────────────────────────────────────────┘
```

### Communication Protocol (SS2 + CW305 Registers)

The FPGA uses ChipWhisperer's SimpleSerial2 (SS2) protocol over UART at 230400 baud with COBS encoding and CRC. The Python `CW305` target class provides `fpga_write()`/`fpga_read()` for register access.

| Register | Address | Size | Description |
|----------|---------|------|-------------|
| CLKSETTINGS | 0x00 | 1 | Clock mux configuration |
| USER_LED | 0x01 | 1 | LED control |
| CRYPT_TYPE | 0x02 | 1 | Crypto type (2 = AES) |
| CRYPT_REV | 0x03 | 1 | Crypto revision (5) |
| IDENTIFY | 0x04 | 1 | ID register (0x2E) |
| CRYPT_GO | 0x05 | 1 | Write: start encryption; Read: busy status |
| CRYPT_TEXTIN | 0x06 | 16 | 128-bit plaintext input |
| CRYPT_CIPHEROUT | 0x09 | 16 | 128-bit ciphertext output |
| CRYPT_KEY | 0x0A | 16 | 128-bit key input |

### Pin Mapping (CW312-A35 on CW313)

FPGA Part: **XC7A35T-1CSG324** (324-pin BGA)

| Signal | FPGA Pin | CW313 Pin | Description |
|--------|----------|-----------|-------------|
| `clk` | D15 | HS2/CLKIN | Clock from Husky (7.37 MHz) |
| `clkout` | A13 | — | Clock output (optional) |
| `resetn` | A16 | nRST | Active-low reset (driven by Husky) |
| `rxd` | V11 | IO2 | SS2 UART: Husky TX → FPGA RX |
| `txd` | V10 | IO1 | SS2 UART: FPGA TX → Husky RX |
| `io3` | V12 | IO3 | Unused (active low pulldown) |
| `io4` | V14 | IO4 | Trigger: active high during encrypt |
| `led1` | R1 | — | USB clock heartbeat |
| `led2` | V2 | — | SS2 error indicator |
| `led3` | V5 | — | Crypto clock heartbeat |

**CRITICAL**: Scope TIO must be swapped: `scope.io.tio1='serial_rx'`, `scope.io.tio2='serial_tx'`

### Results Directory

All capture results are saved under `results/`. Use `--results-dir` to organize by experiment:

```bash
python3 cw_aes.py encrypt --random --num-traces 500 --results-dir ../results/cpa_attack_1
python3 cw_aes.py encrypt --random --num-traces 500 --results-dir ../results/cpa_attack_2
```

Output files per experiment:
- **`.mat`**: numpy arrays (traces, plaintexts, ciphertexts, key, metadata)
- **`.json`**: human-readable hex data + per-trace verification status

---

## NIST Test Vectors (AES-128 ECB)

| Key | Plaintext | Expected Ciphertext |
|-----|-----------|-------------------|
| `2b7e151628aed2a6abf7158809cf4f3c` | `6bc1bee22e409f96e93d7e117393172a` | `3ad77bb40d7a3660a89ecaf32466ef97` |
| `000102030405060708090a0b0c0d0e0f` | `00112233445566778899aabbccddeeff` | `69c4e0d86a7b0430d8cdb78070b4c55a` |

---

## Troubleshooting

### SS2 communication issues
- **TIO swap**: Must set `scope.io.tio1='serial_rx'` and `scope.io.tio2='serial_tx'` (FPGA txd is on IO1)
- **Clock order**: Configure `clkgen_freq` before programming, disable `hs2` during SPI, re-enable after
- **Reset**: Drive `scope.io.nrst='low'` then `'high'` after clock is enabled
- **Baud rate**: SS2 uses 230400 baud (pBIT_RATE=32 @ 7.37 MHz); mismatch causes timeouts
- Use `capture/test_ss2.py` for debugging communication issues

### Vivado synthesis fails
- Ensure `fpga/rtl/aes_128.sv` exists (run HLS first)
- Check that pin assignments match your CW312-A35 board revision
- Verify `CFGBVS` and `CONFIG_VOLTAGE` settings

### Ciphertext mismatch
- Verify the AES XLS source matches TinyAES byte ordering
- Check that the key is loaded before sending plaintext
- Run with `--num-traces 1` and compare against known test vectors

---

## License

The AES implementation is derived from [tiny-AES-C](https://github.com/kokke/tiny-AES-C) (public domain / Unlicense).
